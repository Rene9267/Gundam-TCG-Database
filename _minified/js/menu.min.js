const gid=(i)=>document.getElementById(i),qsa=(s)=>document.querySelectorAll(s),qs=(s)=>document.querySelector(s),jp=JSON.parse,js=JSON.stringify,ns=Set,nu=URL,nusp=URLSearchParams,mm=Math.min,mx=Math.max,mr=Math.round,Af=Array.from,MP=Math.PI,Ok=Object.keys,Ov=Object.values;Element.prototype.ael=Element.prototype.addEventListener;c MENU_ITEMS = ['dashboard', 'collection', 'decks', 'profile'];

fn openMenu() {
  c modal = gid('menu-modal');
  c panel = gid('menu-panel');
  modal.style.display = 'block';
  setTimeout(() => {
    panel.style.transform = 'translateX(0)';
  }, 10);
}

fn closeMenu() {
  c modal = gid('menu-modal');
  c panel = gid('menu-panel');
  panel.style.transform = 'translateX(-100%)';
  setTimeout(() => { modal.style.display = 'none'; }, 250);
}

fn openLegal() {
  c modal = gid('legal-modal');
  c panel = gid('legal-panel');
  modal.style.display = 'block';
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      panel.style.opacity = '1';
      panel.style.transform = 'translateX(-50%) translateY(-50%) scale(1)';
    });
  });
}

fn closeLegal() {
  c modal = gid('legal-modal');
  c panel = gid('legal-panel');
  panel.style.opacity = '0';
  panel.style.transform = 'translateX(-50%) translateY(-50%) scale(0.95)';
  setTimeout(() => { modal.style.display = 'none'; }, 200);
}

fn syncMenuActive(tab) {
  for (c id of MENU_ITEMS) {
    c el = gid('menu-' + id);
    if (!el) continue;
    if (id == tab) {
      el.classList.add('text-white', 'border-accent');
      el.classList.remove('text-white/60', 'border-transparent');
    }
      el.classList.add('text-white/60', 'border-transparent');
      el.classList.remove('text-white', 'border-accent');
    }
  }
}
